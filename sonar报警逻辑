方案汇总

报警逻辑放在 server，data 不做报警判断。data 继续负责采集、写 Mongo、上传实时 dataSonarSv；server 在收到 dataSonarSv 后，用内存状态机判断 4/5/6 持续报警，达到条件后触发现有 SONAR_SV_VALUE 报警。

数据流

sonar echo 数据
-> data 解析出 sonar_sv
-> data 写入 Mongo dataSonarSv
-> data 调用 server /api/handleRealDataList
-> server 判断持续报警
-> server 触发 SONAR_SV_VALUE
-> 现有报警保存、弹窗、订阅刷新、前端展示
判断规则

原始数据字段：

dataSonarSv.val
换算：

生物入侵强度 = val + 80
规则：

BLUE   24 <= 生物入侵强度 < 30，持续 2 小时
YELLOW 30 <= 生物入侵强度 < 36，持续 1 小时
RED    生物入侵强度 >= 36，持续 0.5 小时
等价用 val 判断：

BLUE   -56 <= val < -50，持续 2 小时
YELLOW -50 <= val < -44，持续 1 小时
RED    val >= -44，持续 0.5 小时
NORMAL val < -56
报警种类固定使用：

MonitorAlarmType.SONAR_SV_VALUE
计算时机

不是定时任务，不扫 Mongo。

计算时机是：

server 每次收到 /api/handleRealDataList 的 dataSonarSv 批数据时计算
因为 sonar_sv 是时序数据，时间早的先到达，所以每条数据直接推进状态机即可。

核心数据结构

只保存每个设备当前连续段状态：

ConcurrentHashMap<String, SonarSvAlarmState> stateMap;
状态对象：

public class SonarSvAlarmState {
    private String deviceCode;
    private MonitorAlarmLevel level;
    private Date startTime;
    private Date lastTime;
    private Double minSv;
    private Double maxSv;
    private Double latestSv;
    private Boolean triggered;
}
内存规模：

设备数 * 1 个状态对象
10 个设备最多约 10 条状态，不保存历史数据，不会随时间增长。

状态含义

如果某设备状态是：

level = BLUE
startTime = 08:00
lastTime = 09:30
含义是：

该设备从 08:00 到 09:30 收到的每条 sonar_sv 都处于 BLUE 区间
因为只要中途出现正常值、其他等级或断点过大，状态就会被清空或重置。

状态推进逻辑

每来一条数据：

1. 读取 deviceCode、time、val
2. 根据 val 判断当前等级
3. NORMAL：清空该设备状态
4. 没有状态：创建新连续段
5. 数据断点过大：重置连续段
6. 等级变化：重置连续段
7. 等级相同：更新 lastTime、minSv、maxSv、latestSv
8. 判断 lastTime - startTime 是否达到该等级持续时间
9. 达到且未触发过：触发报警，并标记 triggered = true
等级变化不继承持续时间：

BLUE 持续 50 分钟后进入 YELLOW，YELLOW 从当前时间重新计 1 小时
断点判断

建议保留一个配置，防止无数据期间被误算成连续：

ai:
  saturntech:
    biz:
      alarm:
        sonar-sv:
          max-gap-seconds: 300
含义：

同一设备相邻两条 data.time 间隔超过 5 分钟，则连续中断，重新计时。
接入点

在 server 的：

DataApiController.java (line 196)

加入：

if (CollectName.DATA_SONAR_SV.getValue().equals(dataList.get(0).getString("collectName"))) {
    sonarSvContinuousAlarmService.handle(dataList);
}
新增服务

public interface SonarSvContinuousAlarmService {
    void handle(List<JSONObject> dataList);
}
实现类负责：

过滤 dataSonarSv
逐条推进状态机
触发 SONAR_SV_VALUE 报警
报警触发方式

建议抽一个内部函数复用 /api/alarm 的逻辑，避免重复代码：

void triggerAlarm(AlarmDTO alarmDTO);
触发时构造：

MonitorAlarm alarm = new MonitorAlarm()
    .setAlarmType(MonitorAlarmType.SONAR_SV_VALUE)
    .setAlarmLevel(state.getLevel())
    .setStatus(MonitorAlarmStatus.OPEN)
    .setDeviceCode(state.getDeviceCode())
    .setAlarmTime(state.getStartTime())
    .setEndTime(state.getLastTime())
    .setValue(state.getLatestSv())
    .setMinValue(state.getMinSv())
    .setMaxValue(state.getMaxSv())
    .setContent(buildContent(state));
然后走现有保存和推送：

monitorAlarmService.saveOrUpdate(alarm);
monitorAlarmSendService.sendBizAlarm(alarm);
monitorAlarmSubscribeService.sendAllByDeviceCode(deviceCode);
去重策略

同一设备、同一等级、同一连续段只触发一次。

triggered = false -> 达到持续时间 -> 触发报警 -> triggered = true
后续同等级数据继续来，不再重复创建报警。
恢复正常、等级变化、断点过大后，重新开始下一段。

测试场景

需要覆盖：

BLUE 连续 2 小时触发
YELLOW 连续 1 小时触发
RED 连续 30 分钟触发
未达到持续时间不触发
中途 NORMAL 清空状态
等级变化重置状态
数据断点超过 max-gap-seconds 重置状态
同一连续段不重复触发
这个方案是当前前提下最合适的：逻辑在 server，不扫库，不保存历史点，内存占用固定，按时序数据流式判断
